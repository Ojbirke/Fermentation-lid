# Never commit real credentials — fill these in locally only
secrets.h
*.local.*
.env

# Vendored ECharts, if you choose a local copy over the CDN
dashboard/echarts.min.js

# Reference server storage — contains lid tokens and your recorded sessions
server/data/

# Recorded sessions
*.csv

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
